"use strict";
const GenSequence_1 = require("../src/GenSequence");
function fibonacci() {
    function* fib() {
        let [a, b] = [0, 1];
        while (true) {
            yield b;
            [a, b] = [b, a + b];
        }
    }
    return GenSequence_1.genSequence(fib());
}
exports.fibonacci = fibonacci;
function fib(n) {
    return fibonacci()
        .take(n) // Take n from the fibonacci sequence
        .toArray(); // Convert it into an array
}
exports.fib = fib;
const fib5 = fib(5); // [1, 1, 2, 3, 5]
//# sourceMappingURL=fibonacci.js.map